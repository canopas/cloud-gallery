name: data
description: "Cloud gallery backend service module"
version: 0.0.1

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # services
  googleapis: ^12.0.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # lints
  flutter_lints: ^2.0.0
  riverpod_lint: ^2.1.1

  # code generation
  build_runner: ^2.3.3
  freezed: ^2.4.6

flutter:

